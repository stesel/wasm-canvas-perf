{
  "name": "webgpu-canvas",
  "version": "0.1.1",
  "description": "webgpu-canvas test",
  "scripts": {
    "build": "rm -rf ./dist && tsc && cp -a ./page/. ./dist",
    "start": "serve ./dist"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.50",
    "serve": "^14.2.4",
    "typescript": "^5.6.3"
  }
}
