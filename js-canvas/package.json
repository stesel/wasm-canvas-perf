{
  "name": "js-canvas",
  "version": "0.1.0",
  "description": "js-canvas test",
  "scripts": {
    "build": "rm -rf ./dist && tsc && cp -a ./page/. ./dist",
    "start": "serve ./dist"
  },
  "devDependencies": {
    "serve": "^14.1.2",
    "typescript": "^4.9.4"
  }
}
