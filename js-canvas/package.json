{
  "name": "js-canvas",
  "version": "0.1.1",
  "description": "js-canvas test",
  "scripts": {
    "build": "rm -rf ./dist && tsc && cp -a ./page/. ./dist",
    "start": "serve ./dist"
  },
  "devDependencies": {
    "serve": "^14.2.4",
    "typescript": "^5.8.3"
  }
}
